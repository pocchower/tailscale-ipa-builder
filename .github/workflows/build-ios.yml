name: Build IPA on GitHub macOS Runner

on:
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-13
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Hello World step
      run: echo "Build system is live"

